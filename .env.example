# Copy to `.env` and adjust locally — never commit real secrets (see root `.gitignore`).
# Template consumed by `docker compose` (see `README.docker.md`).

# PostgreSQL superuser name created on first boot.
POSTGRES_USER=cbt_app

# Required in `.env` — no safe default; use a strong value outside production.
POSTGRES_PASSWORD=change_me_local_only

# Database name created on first boot.
POSTGRES_DB=cognitive_bias_tester

# Host port mapped to container 5432. Default 5433 avoids clashing with a local PostgreSQL on 5432.
POSTGRES_PORT=5433

# Async SQLAlchemy URL (hostname `db` and port `5432` are inside the Docker network).
# Keep credentials aligned with POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB above.
DATABASE_URL=postgresql+asyncpg://cbt_app:change_me_local_only@db:5432/cognitive_bias_tester

# Async engine pool size (see backend/src/db/session.py).
DB_POOL_SIZE=5

# Reserved for backend HTTP (FastAPI) in task T030.
BACKEND_PORT=8000

# Reserved for Vite dev server in task T050.
FRONTEND_PORT=5173
